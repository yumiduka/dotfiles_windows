# dotfiles

## macOS

macOS用セットアップファイル。

初回実行スクリプトは _install.sh_ 。

## PowerShell

Windows PowerShell / PowerShell Core用セットアップファイル。

Windows環境以外も対応。

初回実行スクリプトは _install.ps1_ 。

Windows以外の環境でPowerShell Coreを使う場合は、それぞれの初回実行スクリプトから _PowerShell.ps1_ を実行する。
